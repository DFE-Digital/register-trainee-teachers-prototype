<h3 class="govuk-heading-s">The record is incomplete</h3>

<p class="govuk-body">You need to provide further information:</p>

<ul class="govuk-list govuk-list--bullet">
  {% if record | needsPlacementDetails %}
    <li>
      <a href='{{ recordPath + "/placements/confirm" | addReferrer(referrer) }}' class="govuk-link">Complete placement details</a>
    </li>
  {% endif %}
  
  {% if not record.trainingDetails.commencementDate | length > 0 %}
     <a href='{{ recordPath + "/trainee-start-date" | addReferrer(referrer) }}' class="govuk-link">Provide a trainee start date</a>
    </li>
  {% endif %}
</ul>
