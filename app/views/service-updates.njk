
{% extends "_templates/_page.njk" %}
{% set backLink = '/start-page' %}

{% set pageHeading = 'News and updates' %}

{% block pageNavigation %}
  {% if backLink %}
    {{ govukBackLink({
      text: backText,
      href: "javascript: window.history.go(-1)"
    }) }}
  {% endif %}
{% endblock %}

{% block content %}

<div class="govuk-grid-row">
  <div class="govuk-grid-column-full">
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds-from-desktop">
        <h1 class="govuk-heading-xl">{{pageHeading}}</h1>
        <h2 class="govuk-heading-m">Contents</h2>
        <ul class="govuk-list govuk-list--dash govuk-!-margin-bottom-8 govuk-!-margin-left-3">
          {% for update in data.serviceUpdates.items %}
            <li><a class="govuk-link" href="#{{ update.title | slugify }}">{{ update.title }}</a></li>
          {% endfor %}
        </ul>
        {% for update in data.serviceUpdates.items %}
          <hr class="govuk-section-break govuk-section-break--l govuk-section-break--visible">
          {% include "_includes/app-service-updates.njk" %}
        {% endfor %}
      </div>
    </div>
  </div>
</div>

{% endblock %}
