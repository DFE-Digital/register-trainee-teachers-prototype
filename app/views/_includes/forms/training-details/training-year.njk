{% set years = [] %}

{% for year in data.years.academicYears %}
  {% set years = years | push({
    text: year
    }) %}
{% endfor %}

{% include "_includes/trainee-name-caption.njk" %}

{{ govukRadios({
  fieldset: {
    legend: {
      text: pageHeading,
      isPageHeading: true,
      classes: "govuk-fieldset__legend--l"
    }
  },
  items: years
} | decorateAttributes(record, "record.academicYear"))}}

{{ govukButton({
  text: "Continue"
}) }}


